# RQ1 — Accuracy Evaluation

Run GreenField against the 20 synthetic projects in `test/benchmarks/synthetic/` and measure precision and recall.

## Metrics
- **Precision** = true dead fields flagged / all fields flagged
- **Recall** = true dead fields flagged / all actual dead fields
