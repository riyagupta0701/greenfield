// Person A — URL normalization
// Normalizes :id, {id}, <id> → canonical form for matching

export function normalizeUrl(url: string): string {
  // TODO: unify param styles (:id, {id}, <id>) to a single canonical form
  return url;
}
