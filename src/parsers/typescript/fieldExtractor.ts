// Person B — Frontend field extraction
// Extracts fields from fetch/axios bodies, JSON.stringify({...}), form objects
import { Field } from '../../types';

export function extractFields(filePath: string): Field[] {
  // TODO: use ts-morph to extract defined fields from frontend request bodies
  return [];
}
