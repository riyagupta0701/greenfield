// Person C — Java backend field extraction
// Extracts fields from Spring @ResponseBody classes and DTOs
import { Field } from '../../types';

export function extractFields(filePath: string): Field[] {
  // TODO: use tree-sitter to extract defined fields from Spring response objects
  return [];
}
