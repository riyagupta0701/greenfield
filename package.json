{
  "name": "greenfield",
  "displayName": "GreenField",
  "description": "Detects unused JSON fields between frontend and backend codebases.",
  "version": "0.0.1",
  "publisher": "greenfield",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other",
    "Linters"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:greenfield.scanWorkspace",
    "workspaceContains:**/*"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "greenfield.scanWorkspace",
        "title": "GreenField: Scan Workspace"
      },
      {
        "command": "greenfield.showEndpoints",
        "title": "GreenField: Show Endpoint Registry"
      }
    ],
    "configuration": {
      "title": "GreenField",
      "properties": {
        "greenfield.include": {
          "type": "array",
          "default": [
            "**/*.{ts,tsx,js,jsx,py,java}"
          ],
          "description": "Glob patterns for files scanned by GreenField."
        },
        "greenfield.exclude": {
          "type": "array",
          "default": [
            "**/node_modules/**",
            "**/dist/**",
            "**/build/**",
            "**/.next/**",
            "**/coverage/**",
            "**/__pycache__/**",
            "**/.venv/**",
            "**/target/**"
          ],
          "description": "Glob patterns excluded from GreenField scans."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@types/vscode": "^1.110.0",
    "typescript": "^5.4.5"
  }
}
